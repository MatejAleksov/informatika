# Vnesemo "tabelo"
array = [-6, 10, 7, -11, 0, 3, 10, -5]
# Določimo kaj naj program pregleduje
for num in array:
    # Če je številka manjša kot 0 se izvede sledeči del
    if num < 0:
        # Številke, ki so manjše kot 0 se izpisujejo.
        print(num)
